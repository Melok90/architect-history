# Стартовая среда — AI Native Designer Quick Start

Привет. Эта папка — твоя готовая рабочая среда для четырёх уроков мини-курса. Внутри уже всё настроено: команды, шаблоны, конфиг MCP. Тебе остаётся только пройти уроки.

## Что внутри

```
.opencode/
└── commands/
    ├── brief.md              ← /brief — структурированный диалог по пяти категориям, на выходе бриф
    ├── research.md           ← /research — конкурентный анализ через встроенный веб-поиск
    └── prd.md                ← /prd — финальная сборка одностраничного PRD

inputs/
└── idea.md                   ← сюда положи свою идею (или возьми из ideas.md)

templates/
└── prd_oneshot.md            ← шаблон финального PRD

outputs/                      ← сюда падают артефакты по ходу работы

opencode.json                 ← конфиг MCP Figma (для Урока 4)
ideas.md                      ← 5 готовых идей продуктов, если у тебя нет своей
```

## Первый запуск

1. **Открой эту папку в OpenCode** как рабочий проект.
2. **Выбери модель в OpenCode.** Любая бесплатная из OpenCode Zen с поддержкой tool-use подойдёт. Рекомендация на момент записи курса — указана в описании урока.
3. **Положи свою идею в `inputs/idea.md`.** Если идеи нет — открой `ideas.md`, выбери одну, скопируй в `idea.md`.
4. **Запусти команду `/brief`** в OpenCode. Модель начнёт задавать тебе вопросы по пяти категориям. Это и есть Урок 1.

Дальше следуй за уроком 1 — он на странице курса.

## К Уроку 4

Перед началом Урока 4 — создай Personal Access Token (PAT) в Figma. Даня показывает на видео, ты повторяешь. После создания открой `opencode.json` и вставь токен в поле `FIGMA_API_KEY` вместо плейсхолдера. После этого модель сможет читать твои Figma-макеты напрямую.

## Если что-то не работает

- Проверь, что выбранная модель доступна и поддерживает tool-use.
- Если упёрся в лимит — переключись на другую free-модель из OpenCode Zen прямо в интерфейсе.
- Остальные вопросы — в описании урока на платформе курса.
